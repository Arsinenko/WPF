<ContentControl x:Class="CollegeApplication.PageAuthorization"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CollegeApplication"
      mc:Ignorable="d" 
      d:DesignHeight="350" d:DesignWidth="300">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="250"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Авторизация" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="30" FontFamily="Cambria"/>
        
        <StackPanel Grid.Row="1" Orientation="Vertical" HorizontalAlignment="Center" Width="200">
            <TextBlock Text="Логин:"></TextBlock>
            <TextBox x:Name="txtLogin" />
            <TextBlock Text="Пароль:" />
            <PasswordBox x:Name="pswdBox"/>
            <Button Content="Войти" Click="Button_Click"/>

        </StackPanel>
    </Grid>
</ContentControl>
